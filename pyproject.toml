[tool.poetry]
name = "k2test"
version = "0.1.0"
description = "test components"
authors = ["Василь Свідрік <svidryk@corp2.eu>"]
license = "free"
readme = "README.md"

[tool.poetry.dependencies]
python = "3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
